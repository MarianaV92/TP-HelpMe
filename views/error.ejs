<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <link rel="stylesheet" href="/css/styles.css" />
  <title>Erreur <%= status %></title>
  <style>
    body { font-family: Arial, sans-serif; background: #f8d7da; color: #721c24; padding: 2rem; }
    .container { max-width: 600px; margin: auto; background: #f5c6cb; border: 1px solid #f5c2c7; border-radius: 8px; padding: 1.5rem; }
    h1 { margin-top: 0; }
  </style>
</head>
<body>
  <div class="container">
    <h1>Erreur <%= status %></h1>
    <p><%= error.message %></p>
    <% if (error.stack) { %>
      <pre><%= error.stack %></pre>
    <% } %>
    <a href="/">Retour à l'accueil</a>
  </div>
</body>
</html>
